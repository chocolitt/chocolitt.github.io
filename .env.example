# Public widget identifier. The production value is set in GitHub Actions.
PUBLIC_FASTCOMMENTS_TENANT_ID=demo

# Set to preview only for a publicly reachable review build. Production builds omit noindex.
PUBLIC_SITE_ENV=production
