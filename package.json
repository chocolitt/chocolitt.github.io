{
  "name": "daniellitt-site",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=24.19.0 <25",
    "pnpm": "11.19.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro check && astro build",
    "validate": "python3 scripts/validation/check_build.py dist && python3 scripts/validation/check_phase6.py dist && python3 scripts/validation/check_analytics.py dist",
    "preview": "astro preview",
    "site:preview": "pnpm run build && pnpm run validate && astro preview"
  },
  "dependencies": {
    "@astrojs/check": "0.9.10",
    "astro": "7.2.0",
    "typescript": "6.0.3"
  }
}
